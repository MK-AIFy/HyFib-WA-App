{
  "name": "@hyfib/app-server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/main.js",
    "dev": "node --watch dist/main.js",
    "test": "node --test",
    "lint": "eslint src"
  },
  "dependencies": {
    "@hyfib/config": "workspace:*",
    "@hyfib/db": "workspace:*",
    "@hyfib/event-bus": "workspace:*",
    "@hyfib/persistence": "workspace:*",
    "@hyfib/ratelimit": "workspace:*",
    "@hyfib/shared-core": "workspace:*"
  }
}
