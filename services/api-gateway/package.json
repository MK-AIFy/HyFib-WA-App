{
  "name": "@hyfib/api-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "dev": "node --watch dist/index.js",
    "test": "node --test",
    "lint": "echo 'No lint configured for @hyfib/api-gateway'"
  },
  "dependencies": {
    "@hyfib/auth": "workspace:*",
    "@hyfib/config": "workspace:*",
    "@hyfib/persistence": "workspace:*",
    "@hyfib/policy-engine": "workspace:*",
    "@hyfib/shared-core": "workspace:*"
  }
}
